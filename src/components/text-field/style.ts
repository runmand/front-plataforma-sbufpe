import { CSSProperties } from 'react';

export const wrapperStyle: CSSProperties = { position: 'relative' };
export const textField: CSSProperties = { width: '100%' };

export const iconButtonStyle: CSSProperties = {
	position: 'absolute',
	right: 0,
	top: '50%',
	transform: 'translateY(-50%)',
};
